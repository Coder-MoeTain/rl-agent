# Legacy GPU Training Scripts

These scripts are **deprecated**. Use the canonical training entry point instead:

```bash
python -m agents.train --algo ppo
python -m agents.train --algo ppo_per
python -m agents.train --algo multi
```

Scripts in this directory were kept for historical reference only.
